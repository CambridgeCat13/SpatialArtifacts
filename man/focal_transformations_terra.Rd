% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_helper_functions_VisiumHD.R
\name{focal_transformations_terra}
\alias{focal_transformations_terra}
\title{Morphological transformations using terra (optimized for VisiumHD)}
\usage{
focal_transformations_terra(r, min_cluster_size = 5)
}
\arguments{
\item{r}{A \code{\link[terra]{SpatRaster}} object with binary values (1=outlier, 0=normal).}

\item{min_cluster_size}{Minimum size (in bins) for small hole removal.}
}
\value{
A \code{\link[terra]{SpatRaster}} object after applying focal
transformations (fill, outline, star) and small hole removal.
}
\description{
Terra-only implementation of focal operations for connecting outlier regions.
}
\examples{
# Create a dummy binary SpatRaster for demonstration
if (requireNamespace("terra", quietly = TRUE)) {
  r <- terra::rast(matrix(c(0, 0, 0, 0, 1, 0, 0, 0, 0), nrow = 3),
    extent = terra::ext(0, 3, 0, 3)
  )
  # Run the focal transformations
  # result <- focal_transformations_terra(r, min_cluster_size = 5)
}

}
